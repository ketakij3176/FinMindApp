# Not used yet — placeholders for Stage 2 (Google auth) and Stage 4 (database),
# both via Supabase. Copy this file to .env once you create a Supabase project
# and fill in the real values from Project Settings > API.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
